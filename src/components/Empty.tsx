import React from "react";

const Empty = () => {
    return (
        <div className="text-center mt-4">
            <p className="fw-bold fs-2 text-white">Ups! There are no results to show at the moment</p>
        </div>
    );
};

export default Empty;
